아래는 DraftNaver 생성 입력(payload_json)입니다.
이 JSON을 읽고, 네이버 블로그에 그대로 붙여 넣어도 “진짜 포스팅처럼” 읽히는 초안을 작성하세요.

[INPUT_JSON]
{{payload_json}}
[/INPUT_JSON]

[작성 목표]
- CEO/실무자가 “바로 점검/복붙/내부 공유”할 수 있는 글.
- ‘불안 조장’이 아니라 ‘합리적 경각심 + 즉시 실행 가능한 점검’ 톤.

────────────────────────────────────────
[1) 제목 후보(title_candidates)]
- 3~5개 생성.
- 아래 믹스를 지켜라:
  (A) 검색형 1~2개: "<키워드> <대상> <문서/체크리스트/예시>"
  (B) 공유형 1~3개: "실수 방지/리스크 줄이기/시간·비용 절감" 뉘앙스
- selected_candidate.title_search/title_share/primary_keyword/longtail_keywords를 자연스럽게 활용.
- 과장/확정 금지(완벽/무조건/100%/반드시/최고 등).

────────────────────────────────────────
[2) 본문(body_md_lines) — 반드시 아래 순서/구조]
※ body_md_lines는 “한 줄 배열”이다. 각 요소에 \n을 넣지 말고, 문단/섹션 사이에는 반드시 ""(빈 줄)을 넣어라.

[필수 섹션 순서]

0) Hook 블록 (첫 3~5줄)
- 1줄: 강한 첫 문장(질문/숫자/선언 중 택1) — 비용/시간/운영 리스크 관점
- 1~2줄: 공감 + 흔한 실수/누락(경험 기반처럼 보이되 식별정보 금지)
- 1줄: “이 글에서 얻는 것(체크리스트/예시/절차)” 명시
- (SEO) 여기까지 5줄 안에 primary_keyword 또는 longtail 1개를 자연스럽게 1회 포함

1) ## 목차
- 5~7개 항목으로 목차를 bullet로 제공

2) ## TL;DR
- 3줄 요약(각 줄 1문장)
- “무엇이 위험 / 무엇을 하면 줄어듦 / 오늘 제공물” 3요소 포함

3) ## 이런 신호가 보이면 지금 점검이 필요합니다
- 신호 3~5개 (불릿)
- 각 신호는 “관찰 가능한 현상”이어야 함(대표/실무자가 바로 체크 가능)

4) ## 오늘의 핵심 (selected_candidate.format에 따라 1개만 선택)
- 아래 중 하나만 깊게 쓴다. (혼합 금지)

A) format=checklist 또는 smartblock_intent=checklist_template
- 번호 8~12개
- 각 항목은 한 줄에:
  "**무엇을 확인** — 왜 중요한지 / 실무 팁" 형태
- 리스트 전후에는 ""를 넣어 문단이 붙지 않게

B) format=copy_examples 또는 format=template
- “바로 쓰는 예시/템플릿” 2~4개
- 예시는 블록인용(>)으로 1~2줄
- 각 예시 아래 “주의점 1줄” 추가

C) format=step_by_step
- 단계 5~7개
- 각 단계: "Step N) 해야 할 일 — 실무 포인트"

D) format=faq
- Q&A 5개
- 형식:
  "### Q1. ..."
  "A: ..."

5) ## 실무에서 자주 나오는 오해/실수 (experience_seed 반영)
- 최소 3개
- 각 항목은 2~3줄:
  "오해/실수 → 왜 위험 → 대안(바로 할 수 있는 조치)"

6) ## 자료 요청(CTA) — 딱 1개만
- offer_material 또는 selected_candidate.deliverable 기반
- 2문장:
  1) “자료 요청” 문장(무료/할인/상담 유인 금지)
  2) “이 자료가 특히 유용한 사람” 문장(대상/상황 명확히)
- 전화번호/링크/연락 유도 문구 남발 금지

7) ## 디스클레이머
- 2문장 고정:
  - 일반 정보 제공 목적
  - 사안별 상이 + 게시 전 전문가 검토 권고

────────────────────────────────────────
[문단/빈 줄 규칙(매우 중요)]
- 문단 사이에는 반드시 ""(빈 줄)을 넣어라.
- 소제목(##/###) 앞뒤에도 ""를 넣어라.
- 리스트 시작 전후에도 ""를 넣어라.
- 예시(>) 블록 전후에도 ""를 넣어라.

[금지어/회피]
- payload_json.intake.must_avoid에 포함된 표현은 제목/본문 어디에도 쓰지 마라.
- 과장/결과보장/승소율 암시/전관/비방/무료·할인 유인 표현 금지.

[최종 출력]
- 오직 JSON만 반환하라.
