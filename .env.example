# local
LLM_MODE=mock
DEBUG_AGENT=0
# Draft loose mode (PR-1+): draft agents may output free-form text; UI will render draft_md as-is.
DRAFT_LOOSE_MODE=0

# OpenAI (LLM_MODE=openai일 때 사용)
OPENAI_API_KEY=
OPENAI_BASE_URL=https://api.openai.com
OPENAI_MODEL=gpt-5-mini
OPENAI_TIMEOUT_MS=120000
OPENAI_API_STYLE=auto
# chat completions용 (일부 신형 모델은 max_tokens 대신 max_completion_tokens)
OPENAI_MAX_COMPLETION_TOKENS=8000
# responses용 (reasoning tokens 포함)
OPENAI_MAX_OUTPUT_TOKENS=8000
OPENAI_TEMPERATURE=0.2

# DB
DATABASE_URL=
